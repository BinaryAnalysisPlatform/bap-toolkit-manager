(executable
   (name bap_report_main)
   (public_name bap-toolkit)
   (libraries
        bap_report
        bin_prot
        cmdliner
        str
        core_kernel
        core_kernel.caml_unix
        threads.posix)
   (preprocess (pps ppx_bap)))
